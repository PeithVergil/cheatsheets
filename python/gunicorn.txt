####################################
# GUNICORN
####################################

####################################
# Installing Gunicorn
pip install gunicorn

# Running Django with Gunicorn
gunicorn --workers=3 --bind=127.0.0.1:8000 --access-logfile=/path/to/accesslog --error-logfile=/path/to/errorlog chigitalk.wsgi:application
